<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingStart="8dp"
    android:paddingEnd="2dp"
    android:paddingVertical="10dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:foreground="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true">
    <TextView
        android:id="@+id/music_number"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginEnd="14dp"
        app:cardCornerRadius="4dp"
        android:text="1"
        android:textSize="20sp"
        android:gravity="center"/>

    <TextView
        android:id="@+id/music_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:ellipsize="end"
        android:layout_toEndOf="@id/music_number"
        android:layout_toStartOf="@id/item_menu_button"
        android:fontFamily="@font/ibm_plex_sans_bold"
        android:paddingTop="3dp"
        android:text="@string/title"
        android:textSize="16sp"/>

    <TextView
        android:id="@+id/music_artist"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:ellipsize="end"
        android:layout_toEndOf="@id/music_number"
        android:layout_alignBottom="@id/music_number"
        android:layout_toStartOf="@id/item_menu_button"
        android:paddingBottom="3dp"
        android:fontFamily="@font/ibm_plex_sans_regular"
        android:text="@string/artist"
        android:textSize="14sp"/>

    <ImageButton
        android:id="@+id/item_menu_button"
        android:layout_width="40dp"
        android:layout_height="50dp"
        android:layout_alignParentEnd="true"
        app:tint="@color/colorText"
        android:src="@drawable/ic_item__menu"
        android:background="@color/trueTransparenceColor"
        android:contentDescription="@string/ic_menu_dots"/>

</RelativeLayout>